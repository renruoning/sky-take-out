server:
  # 8090现在是sky-gateway的端口，sky-server挪到这个端口，只有网关和其它已注册到Nacos的服务能按服务名找到它——
  # nginx.conf里的upstream原本就指向8090，改成指向gateway之后不需要再改nginx配置
  port: 8091
  tomcat:
    threads:
      # 默认值是200，对这个项目的实际规模（demo/作品集评审，不是真实大流量）明显过量；
      # 显式调小到跟下面Druid连接池数量级匹配的水平，是为了让这两个数字互相呼应，而不是各自随便取一个
      max: 50
      min-spare: 10
    # 线程池打满之后，允许在accept队列里排队等待的连接数，超过这个数直接拒绝而不是无限堆积
    accept-count: 100

spring:
  profiles:
    active: dev
  application:
    name: sky-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  main:
    allow-circular-references: true
  datasource:
    druid:
      driver-class-name: ${sky.datasource.driver-class-name}
      url: jdbc:mysql://${sky.datasource.host}:${sky.datasource.port}/${sky.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${sky.datasource.username}
      password: ${sky.datasource.password}
      # Druid不显式配置时max-active默认只有8——对这个项目来说太小了，前面并发下单测试稍微多开几个连接就可能顶到；
      # 这里按demo规模显式给一组数字，而不是继续依赖框架默认值。真要估算工业级流量下的池子大小，走P5的容量规划方法论
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 3000
  redis:
    host: ${sky.redis.host}
    port: ${sky.redis.port}
    password: ${sky.redis.password}
    database: ${sky.redis.database}
  rabbitmq:
    host: ${sky.rabbitmq.host}
    port: ${sky.rabbitmq.port}
    username: ${sky.rabbitmq.username}
    password: ${sky.rabbitmq.password}
    virtual-host: ${sky.rabbitmq.virtual-host}

mybatis:
  #mapper配置文件
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.sky.entity
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      sky:
        mapper: debug
        service: info
        controller: info

sky:
  jwt:
    # 设置jwt签名加密时使用的秘钥
    admin-secret-key: itcast
    # 设置jwt过期时间 单位毫秒 现在是10小时
    admin-ttl: 36000000
    # 设置前端传递过来的令牌名称
    admin-token-name: token
    # 设置jwt签名加密时使用的秘钥
    user-secret-key: itheima
    # 设置jwt过期时间 单位毫秒 现在是2小时
    user-ttl: 7200000
    # 设置前端传递过来的令牌名称
    user-token-name: authentication
    
  alioss:
    endpoint: ${sky.alioss.endpoint}
    access-key-id: ${sky.alioss.access-key-id}
    access-key-secret: ${sky.alioss.access-key-secret}
    bucket-name: ${sky.alioss.bucket-name}

  wechat:
    appid: ${sky.wechat.appid}
    secret: ${sky.wechat.secret}
    mchid: ${sky.wechat.mchid}
    mchSerialNo: ${sky.wechat.mchSerialNo}
    apiV3Key: ${sky.wechat.apiV3Key}
    privateKeyFilePath: ${sky.wechat.privateKeyFilePath}
    weChatPayCertFilePath: ${sky.wechat.weChatPayCertFilePath}
    notifyUrl: ${sky.wechat.notifyUrl}
    refundNotifyUrl: ${sky.wechat.refundNotifyUrl}

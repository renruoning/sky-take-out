# TODO：评估过、这次不做的项

这份文件收的是"分析清楚了、故意不落地"的条目——跟[REPORT.md](REPORT.md)不是一回事，REPORT.md只收已经做完并验证过的事。这些条目原来直接写在REPORT.md里，从那边搬过来单独放，避免"已完成"和"评估后不做"混在同一份文档里。

每条区分两类：**面试低频**（做了投入产出比不划算，面试大概率不会问到这个粒度）和**面试高频但不需要真做**（面试官考察的是设计能力，不是demo里有没有真搭出来）。

## 面试高频但评估后不需要真做

- **`review/list`索引优化**：分页查询按`shop_id`过滤+`create_time`排序，现在评价数据量小看不出来，量大了会重演`historyOrders`那个问题（`Using filesort`）。
  - **方案**：跟`historyOrders`那次完全同一套打法。第一步加联合索引`(shop_id, create_time DESC)`（短期，offset分页够用时先做这一步，`EXPLAIN`确认`key`走新索引、`Extra`不再出现`Using filesort`）；量真的大到offset分页的`COUNT(*)`开始变贵时，再引入游标分页，这时索引要跟`historyOrders`一样升级成三列`(shop_id, create_time DESC, id DESC)`——两列索引只覆盖排序第一个字段，覆盖不了游标查询`(create_time, id) < (?, ?)`这种tie-break比较，会退化回filesort，这是`historyOrders`那次已经踩过一次的坑，这里不用重新踩。
  - **迁移文件**：照抄`database/migration_orders_user_time_index.sql`/`migration_orders_user_time_id_index.sql`的模式，新增`database/migration_review_shop_time_index.sql`。
  - **验证方式**：`SHOW INDEX`确认索引存在；`EXPLAIN SELECT ... WHERE shop_id=? ORDER BY create_time DESC`确认走新索引且无filesort；如果做到游标分页那一步，要写脚本把某店铺的评价从第一页翻到`hasMore=false`，核对总数、去重、`create_time`严格递减，不能只测单页。
  - 低频原因：知识点和`historyOrders`那条（已做完）完全一样，面试官问完一次不会重复问几乎一样的场景。

- **读写分离**：现在单实例MySQL，所有读写都打主库。
  - **方案**：基础设施上至少1主N从，用MySQL原生binlog复制（异步或半同步）。路由方式两条路：①中间件（ShardingSphere-JDBC/Proxy），配置里配一条读写分离规则，应用代码不用改；②应用层自己维护主/从两个数据源，用`AbstractRoutingDataSource`按"当前是否在写事务里"动态路由——在写事务（`@Transactional`）里或者显式标记的写方法强制走主库，纯查询走从库。
  - **一致性坑**：主从复制延迟会导致"写后立即读"读不到最新数据。这个项目里最容易踩的场景是"下单/支付后立刻查详情"——对策要么强制这类写后读走主库，要么业务上容忍最终一致（历史订单列表晚几秒同步没问题），要么用半同步复制缩小延迟窗口。
  - **对这个项目的映射**：读多写少的接口（菜单浏览、历史订单列表、报表统计）适合分流到从库；写路径（`submit`下单、支付、库存扣减）必须留在主库——尤其是接了Seata Saga之后，分布式事务的执行过程不能允许从库读到中间态。
  - **面试怎么讲**：讲清楚binlog复制原理、中间件vs应用层路由的取舍、复制延迟怎么处理，就是完整答案，不需要真搭一套主从。
  - **真做的话投入量级**：接近这次做的Redis Sentinel高可用那个量级，对这个demo项目边际收益太低。

- **订单表分库分表规划**：`orders`表现在2万+行，单表还没到真正需要分片的量级，这条本来就是设计题。
  - **方案（拆分维度选择）**：优先按`user_id`哈希分片——用户查自己历史订单（`historyOrders`）是最高频场景，同一用户的订单落在同一分片，不用跨分片聚合。反例对比：按`shop_id`分片对管理端"某店铺全部订单"友好，但对用户端"我的订单"不友好；按时间范围分表对归档友好，但对"用户历史订单"这种跨时间查询不友好。这个项目下单场景以用户视角为主，`user_id`哈希更合理，冷数据（比如1年前的订单）可以另外按时间归档到历史库。
  - **中间件选型**：ShardingSphere-JDBC（内嵌进应用，不用额外部署代理层）vs ShardingSphere-Proxy/Mycat（独立部署，对应用透明但多一层网络跳转和运维成本）。这个项目体量小，优先考虑JDBC模式。
  - **全局唯一ID**：`orders.id`现在是自增主键，分库分表后自增会冲突，需要换成雪花算法或美团Leaf这类分布式ID生成器。
  - **非分片键查询**：分片键选`user_id`之后，管理端按订单号/手机号查询会退化成全分片广播扫描——通常另建一张"订单号→分片路由"的索引表，或者引入ES处理这类非分片键查询。
  - **数据迁移**：现有数据从单库分批灌进分片库，期间要考虑不停机方案（灰度双写+校验+切流，或者用Canal监听binlog做增量同步）——跟这个项目之前拆分微服务时用的"原样搬迁+回填"思路是同一个方向，但规模和不停机要求都更高。
  - **面试怎么讲**：讲清楚为什么选`user_id`哈希、非分片键查询怎么处理、全局ID怎么生成、怎么不停机迁移，这套设计的完整程度比"demo里真跑一个2分片集群"更重要。
  - 原来记在`TODO.md`里的原话是"先不用真的拆，先想清楚拆分维度"，真去接分库分表中间件、做数据迁移，投入和作品集场景完全不成比例。

- **大促流量脉冲专项预案演练**：依赖先有一套能模拟真实脉冲流量模型的压测工具链（不是现在这个固定并发循环打的Node脚本，而是能模拟"秒杀开场瞬间流量陡增几十倍再快速回落"这种脉冲曲线），这个前置条件还没搭。
  - **方案（五个维度）**：
    1. **预热热点缓存**：大促前主动把可预期的热门菜品/店铺数据刷进Redis，避免开场瞬间大量请求穿透到DB——现有的逻辑过期+互斥锁（`LogicalExpireCache`）已经防住了击穿，但冷启动第一波瞬时并发依然会有一小段延迟，预热能把这段也抹掉。
    2. **临时限流阈值调整**：把有限的连接池/线程池资源让给下单核心链路——非核心接口（报表统计、AI客服）在大促窗口临时限流或熔断，保核心接口。
    3. **热点行写竞争削峰**：`StockServiceImpl.deductStock()`现在是逐条UPDATE，秒杀场景下同一商品会有大量并发打同一行，数据库行锁天然排队、可能锁等待超时——真做的话要考虑Redis预扣库存（原子`DECR`先做一层预检）或者MQ削峰（下单请求先入队，后台异步慢慢处理，不让高峰期数据库直接扛全部并发写）。
    4. **弹性扩容**：结合P6已经验证过的能力（Nacos+LoadBalancer自动把新实例纳入轮询池），大促前手动多起几个实例、潮退后缩回去——现在没有K8s HPA这种自动伸缩，是手动扩容。
    5. **监控告警前置**：需要先有Prometheus/Grafana（P0里"指标监控"那条也没做）才能在演练时实时看到QPS/延迟/错误率曲线，判断预案有没有生效。
  - **面试怎么讲**：预热缓存、限流降级、热点行写削峰、弹性扩容、可观测性这五个维度讲清楚就是完整答案，不需要真的拉一次大促演练。

## 面试低频考点

- **WebSocket连接状态迁移到Redis**：现在连接信息存在单个JVM实例的内存`Map`里，一旦水平扩到多实例，通知可能推不到人在另一个实例上的连接。真做的话：状态搬进Redis，通知改成发MQ或Redis Pub/Sub广播给所有实例，每个实例只推自己本地持有的连接。低频原因：更像"多实例实时推送"这种系统设计题里的一个子点，很少被单独拎出来问。
- **RabbitMQ高可用**：现在单实例，挂了会丢通知（不影响下单，链路已经fail-open）。真做的话：换成镜像队列或仲裁队列集群，纯部署配置，不用改代码。低频原因：面试常问"怎么保证消息不丢/不重复"（这部分已经做了），但"broker本身怎么搭高可用集群"这种运维细节问得少。
- **报表查询迁移出业务库**：报表现在直接查`orders`表聚合，量大了会拖慢下单主库。真做的话：把数据同步到ClickHouse等OLAP，报表查询完全走独立库，跟交易库隔离。低频原因：偏数据平台/中台方向，普通后端面试很少问到这个粒度。
- **评分聚合短TTL缓存**：`/user/shop/list`每次都实时调评价服务问评分，没有缓存（故意的，为了评分能马上更新）。真做的话：给评分结果单独加一个10-30秒的短TTL缓存，兼顾"减少Feign调用"和"评分别太旧"。低频原因：这是这个项目自己的业务权衡细节，不是通用知识点。
- **JVM/容器资源限制**：容器之间现在没有CPU/内存边界，高并发下互相抢资源。真做的话：`java -jar`加`-Xmx`限制堆大小，`docker run`加`--cpus`/`--memory`限制。低频原因：JVM调优本身是常考点（堆/GC原理），但"容器里具体怎么配资源限制"这种运维配置细节问得少。
